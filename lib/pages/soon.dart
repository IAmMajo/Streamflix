import 'package:flutter/material.dart';

class SoonPage extends StatelessWidget {
  const SoonPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Bald', style: Theme.of(context).textTheme.displayLarge),
    );
  }
}
